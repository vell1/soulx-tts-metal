# Web 服务框架
fastapi
uvicorn[standard]
gradio

# 深度学习和模型推理（基于官方依赖 + Mac 支持）
torch==2.7.1
torchaudio==2.7.1
transformers==4.57.1
accelerate==1.10.1
triton>=3.0.0; sys_platform != "darwin"
diffusers
onnxruntime
onnxruntime-gpu; sys_platform != "darwin"

# 音频处理
soundfile
librosa
scipy
einops
torchcodec

# NLP
s3tokenizer

# 其他依赖
numpy
pydantic

# 注意: SoulX-Podcast 模块已集成到项目中 (./soulxpodcast/)